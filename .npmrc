# better-auth declares optional peer deps (@tanstack/react-start, vitest) for
# integrations we don't use; their transitive `vite` ranges conflict with our
# vitest's vite. These peers are optional, so relax peer resolution. Applies to
# local installs and CI `npm ci` alike for reproducibility.
legacy-peer-deps=true
